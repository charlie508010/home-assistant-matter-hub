diff --git a/dist/cjs/behavior/system/commissioning/CommissioningServer.js b/dist/cjs/behavior/system/commissioning/CommissioningServer.js
index 7223fcaa25378958a20893bd1af9ecd9bd13d184..ab1ce49a5d8d50b300328e0c76d518646a7faf48 100644
--- a/dist/cjs/behavior/system/commissioning/CommissioningServer.js
+++ b/dist/cjs/behavior/system/commissioning/CommissioningServer.js
@@ -118,7 +118,10 @@ class CommissioningServer extends import_Behavior.Behavior {
     }
   }
   #triggerFactoryReset() {
-    this.env.runtime.add(this.endpoint.erase().catch((e) => import_general.MutexClosedError.accept(e)));
+    logger.info("delaying factory reset after last fabric removal by 30s");
+    this.env.runtime.add(
+      new Promise((resolve) => setTimeout(resolve, 3e4)).then(() => this.endpoint.erase()).catch((e) => import_general.MutexClosedError.accept(e))
+    );
   }
   #monitorFailsafe(failsafe) {
     if (this.internal.unregisterFailsafeListener) {
@@ -177,6 +180,7 @@ class CommissioningServer extends import_Behavior.Behavior {
       );
     }
     await this.env.get(import_protocol.DeviceCommissioner).allowBasicCommissioning();
+    logger.info("commissioning window opened after reset/remove");
     this.initiateCommissioning();
   }
   /**
diff --git a/dist/cjs/behaviors/descriptor/DescriptorServer.js b/dist/cjs/behaviors/descriptor/DescriptorServer.js
index a6786485017dd7789ee6a8fc4f0d9033dcdb2ae2..7cabdcf15325b8e045d97080ad1e672d8c902dbd 100644
--- a/dist/cjs/behaviors/descriptor/DescriptorServer.js
+++ b/dist/cjs/behaviors/descriptor/DescriptorServer.js
@@ -167,7 +167,7 @@ class DescriptorServer extends import_DescriptorBehavior.DescriptorBehavior {
     }
     numbers.sort();
     if (this.state.partsList.length === numbers.length) {
-      let i = numbers.length;
+      let i = 0;
       for (; i < numbers.length; i++) {
         if (this.state.partsList[i] !== numbers[i]) {
           break;
@@ -186,9 +186,12 @@ class DescriptorServer extends import_DescriptorBehavior.DescriptorBehavior {
    */
   get #serverList() {
     const list = new Array();
-    for (const type of Object.values(this.endpoint.behaviors.supported)) {
+    if (this.endpoint.number === 0) {
+      return [40, 31, 63, 48, 60, 62, 51, 49, 42, 70, 29];
+    }
+    for (const type of this.endpoint.behaviors.active) {
       const clusterId = type.cluster?.id;
-      if (clusterId) {
+      if (clusterId !== void 0) {
         list.push(clusterId);
       }
     }
diff --git a/dist/cjs/behaviors/operational-credentials/OperationalCredentialsServer.js b/dist/cjs/behaviors/operational-credentials/OperationalCredentialsServer.js
index e1655864865d84eae9f50c0c0eb79662ae3f189d..37b56caf757941e164a5be427ed1e603796954d3 100644
--- a/dist/cjs/behaviors/operational-credentials/OperationalCredentialsServer.js
+++ b/dist/cjs/behaviors/operational-credentials/OperationalCredentialsServer.js
@@ -37,6 +37,7 @@ var import_VendorIdVerification = require("./VendorIdVerification.js");
  * SPDX-License-Identifier: Apache-2.0
  */
 const logger = import_general.Logger.get("OperationalCredentials");
+const hamhLastFabricRemovalCleanup = /* @__PURE__ */ new WeakMap();
 import_operational_credentials.OperationalCredentials.Cluster.commands = {
   ...import_operational_credentials.OperationalCredentials.Cluster.commands,
   attestationRequest: (0, import_types.Command)(
@@ -297,6 +298,7 @@ class OperationalCredentialsServer extends import_OperationalCredentialsBehavior
   }
   async removeFabric({ fabricIndex }) {
     (0, import_protocol.assertRemoteActor)(this.context);
+    logger.info(`removeFabric requested fabricIndex=${fabricIndex}`);
     const fabric = this.env.get(import_protocol.FabricManager).maybeFor(fabricIndex);
     if (fabric === void 0) {
       return {
@@ -305,7 +307,29 @@ class OperationalCredentialsServer extends import_OperationalCredentialsBehavior
       };
     }
     await this.context.transaction.rollback();
-    await fabric.leave(this.context.exchange);
+    const fabricManager = this.env.get(import_protocol.FabricManager);
+    if (fabricManager.fabrics.length === 1) {
+      const activeCleanup = hamhLastFabricRemovalCleanup.get(fabricManager);
+      if (activeCleanup !== void 0) {
+        logger.info(`last fabric removal already in progress fabricIndex=${fabricIndex}`);
+        return {
+          statusCode: import_operational_credentials.OperationalCredentials.NodeOperationalCertStatus.Ok,
+          fabricIndex
+        };
+      }
+      logger.info(`delaying last fabric removal cleanup by 5s fabricIndex=${fabricIndex}`);
+      const cleanup = new Promise((resolve) => setTimeout(resolve, 5e3)).then(() => fabric.leave()).catch((error) => {
+        logger.warn(`removeFabric cleanup for fabricIndex=${fabricIndex} completed with stale session errors`, error);
+      }).finally(() => hamhLastFabricRemovalCleanup.delete(fabricManager));
+      hamhLastFabricRemovalCleanup.set(fabricManager, cleanup);
+    } else {
+      try {
+        await fabric.leave(this.context.exchange);
+      } catch (error) {
+        logger.warn(`removeFabric cleanup for fabricIndex=${fabricIndex} completed with stale session errors`, error);
+      }
+    }
+    logger.info(`removeFabric succeeded statusCode=0 fabricIndex=${fabricIndex}`);
     return {
       statusCode: import_operational_credentials.OperationalCredentials.NodeOperationalCertStatus.Ok,
       fabricIndex
diff --git a/dist/cjs/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.js b/dist/cjs/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.js
index 81870836236328447186932995fe23d975ebaca9..66c5eed17b73a6e17af15bc3df8ebb93948d5f1f 100644
--- a/dist/cjs/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.js
+++ b/dist/cjs/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.js
@@ -42,6 +42,8 @@ const logger = import_general.Logger.get("OtaSoftwareUpdateRequestorServer");
 const OTA_BDX_MAX_BLOCK_SIZE_TCP = 8192;
 const OTA_BDX_MAX_BLOCK_SIZE_NON_TCP = 1024;
 const MAX_BUSY_RETRIES = 3;
+let hamhRuntimeBusyRetryCount = 0;
+let hamhRuntimeBusySuppressed = false;
 var ScheduleReason = /* @__PURE__ */ ((ScheduleReason2) => {
   ScheduleReason2[ScheduleReason2["NewQuery"] = 0] = "NewQuery";
   ScheduleReason2[ScheduleReason2["NoUpdateAvailable"] = 1] = "NoUpdateAvailable";
@@ -83,6 +85,7 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
       this.state.updateState = import_ota_software_update_requestor.OtaSoftwareUpdateRequestor.UpdateState.Idle;
       this.state.updateStateProgress = null;
     }
+    this.state.activeOtaProviders = [];
     (await this.agent.load(import_descriptor.DescriptorServer)).addDeviceTypes("OtaRequestor");
     this.reactTo(this.events.updatePossible$Changed, this.#enableOrDisableUpdates);
     this.reactTo(this.events.defaultOtaProviders$Changing, this.#assertDefaultOtaProviders);
@@ -109,17 +112,6 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
   }
   /** When we come back online after an update, we notify that the update was applied */
   async #online() {
-    if (this.state.activeOtaProviders.length === 0 && this.state.defaultOtaProviders.length > 0) {
-      for (const provider of this.state.defaultOtaProviders) {
-        await this.#addActiveOtaProvider(provider);
-      }
-      logger.debug(`No active OTA providers, adding ${this.state.activeOtaProviders.length} default providers`);
-    }
-    if (this.state.activeOtaProviders.length > 0) {
-      this.#scheduleUpdateQuery(
-        this.state.updateInProgressDetails === null ? (0, import_general.Seconds)(Math.floor(Math.random() * 599) + 120) : void 0
-      );
-    }
     if (this.state.updateInProgressDetails !== null) {
       await this.#handlePreviousUpdateOnStart(this.state.updateInProgressDetails);
       this.state.updateInProgressDetails = null;
@@ -242,7 +234,6 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
       );
     }
     const provider = { fabricIndex, providerNodeId, endpoint };
-    await this.#addActiveOtaProvider(provider, metadataForNode);
     logger.info(
       `${announcementReason === import_ota_software_update_requestor.OtaSoftwareUpdateRequestor.AnnouncementReason.UrgentUpdateAvailable ? "Urgent " : ""}OTA Provider announcement received:`,
       import_general.Diagnostic.dict({
@@ -251,8 +242,19 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
         vendorId
       })
     );
-    const peerAddress = (0, import_protocol.PeerAddress)({ nodeId: providerNodeId, fabricIndex });
-    await import_Node.Node.forEndpoint(this.endpoint).peers.forAddress(peerAddress);
+    if (!this.#isLocalFabric(fabricIndex)) {
+      this.#logIgnoringNonLocalOtaProvider(provider);
+      return;
+    }
+    await this.#addActiveOtaProvider(provider, metadataForNode);
+    if (hamhRuntimeBusySuppressed) {
+      logger.debug(
+        `Ignoring OTA provider announcement because Busy retries are suppressed for this bridge runtime.`,
+        import_general.Diagnostic.dict(provider)
+      );
+      this.#markActiveOtaProviderNoUpdate(provider);
+      return;
+    }
     if (announcementReason !== import_ota_software_update_requestor.OtaSoftwareUpdateRequestor.AnnouncementReason.SimpleAnnouncement) {
       const delay = (0, import_general.Seconds)(Math.floor(Math.random() * 599) + 1);
       logger.info(`Scheduling urgent update query in`, delay);
@@ -261,37 +263,69 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
       this.#scheduleUpdateQuery();
     }
   }
-  /** Adds or updates an active OTA provider entry for a fabric index */
-  async #addActiveOtaProvider(provider, metadataForNode) {
-    const { fabricIndex, providerNodeId } = provider;
-    const activeProviderIndex = this.state.activeOtaProviders.findIndex(
-      (p) => p.location.fabricIndex === fabricIndex && p.location.providerNodeId === providerNodeId
+  #locationKey({ fabricIndex, providerNodeId, endpoint }) {
+    return `${fabricIndex}:${providerNodeId}:${endpoint}`;
+  }
+  #isSameProviderLocation(a, b) {
+    return this.#locationKey(a) === this.#locationKey(b);
+  }
+  #plainProviderLocation({ fabricIndex, providerNodeId, endpoint }) {
+    return { fabricIndex, providerNodeId, endpoint };
+  }
+  #isLocalFabric(fabricIndex) {
+    const serverNode = import_Node.Node.forEndpoint(this.endpoint);
+    return Object.values(serverNode.state.commissioning.fabrics).some((fabric) => fabric.fabricIndex === fabricIndex);
+  }
+  #logIgnoringNonLocalOtaProvider({ fabricIndex, providerNodeId, endpoint }) {
+    logger.warn(
+      `Ignoring OTA provider announcement for unknown/non-local fabricIndex=${fabricIndex}, providerNodeId=${providerNodeId}, endpoint=${endpoint}`
     );
-    const activeProviderEntry = {
-      location: provider,
-      previousQueryTimestamp: null,
-      // We set to null to try this provider first next time
-      metadataForNode
-    };
-    if (activeProviderIndex === -1) {
-      this.state.activeOtaProviders.push(activeProviderEntry);
-    } else {
-      this.state.activeOtaProviders[activeProviderIndex] = activeProviderEntry;
+  }
+  #preferOtaProviderEntry(candidate, current) {
+    const candidateTimestamp = candidate.previousQueryTimestamp;
+    const currentTimestamp = current.previousQueryTimestamp;
+    if (candidateTimestamp === null) {
+      return false;
+    }
+    if (currentTimestamp === null) {
+      return true;
+    }
+    return candidateTimestamp > currentTimestamp;
+  }
+  #deduplicateActiveOtaProviders() {
+    const deduplicated = /* @__PURE__ */ new Map();
+    for (const provider of this.state.activeOtaProviders) {
+      const key = this.#locationKey(provider.location);
+      const current = deduplicated.get(key);
+      if (current === void 0 || this.#preferOtaProviderEntry(provider, current)) {
+        deduplicated.set(key, provider);
+      }
+    }
+    if (deduplicated.size !== this.state.activeOtaProviders.length) {
+      this.state.activeOtaProviders = [...deduplicated.values()];
+      logger.info(`Deduplicated active OTA providers to ${this.state.activeOtaProviders.length} entries.`);
+    }
+  }
+  /** Adds or updates an active OTA provider entry for a provider location */
+  async #addActiveOtaProvider(provider, metadataForNode) {
+    void metadataForNode;
+    const plainProvider = this.#plainProviderLocation(provider);
+    const { fabricIndex, providerNodeId } = plainProvider;
+    if (!this.#isLocalFabric(fabricIndex)) {
+      this.#logIgnoringNonLocalOtaProvider(plainProvider);
+      return;
     }
+    this.state.activeOtaProviders = [];
+    this.internal.selectedProviderLocation = plainProvider;
     const peerAddress = (0, import_protocol.PeerAddress)({ nodeId: providerNodeId, fabricIndex });
     await import_Node.Node.forEndpoint(this.endpoint).peers.forAddress(peerAddress);
   }
   /** Removes an OTA provider from the active and default list */
   #removeOtaProvider(provider) {
-    const { fabricIndex, providerNodeId } = provider;
-    const activeProviderIndex = this.state.activeOtaProviders.findIndex(
-      (p) => p.location.fabricIndex === fabricIndex && p.location.providerNodeId === providerNodeId
-    );
-    if (activeProviderIndex !== -1) {
-      this.state.activeOtaProviders.splice(activeProviderIndex, 1);
-    }
+    const { fabricIndex, providerNodeId, endpoint } = provider;
+    this.state.activeOtaProviders = [];
     const defaultProviderIndex = this.state.defaultOtaProviders.findIndex(
-      (p) => p.fabricIndex === fabricIndex && p.providerNodeId === providerNodeId
+      (p) => p.fabricIndex === fabricIndex && p.providerNodeId === providerNodeId && p.endpoint === endpoint
     );
     if (defaultProviderIndex !== -1) {
       this.state.defaultOtaProviders.splice(defaultProviderIndex, 1);
@@ -299,18 +333,8 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
   }
   /**  Marks an OTA provider as active but with no update available (timestamp 0) */
   #markActiveOtaProviderNoUpdate(provider) {
-    const { fabricIndex, providerNodeId } = provider;
-    const activeProviderIndex = this.state.activeOtaProviders.findIndex(
-      (p) => p.location.fabricIndex === fabricIndex && p.location.providerNodeId === providerNodeId
-    );
-    if (activeProviderIndex !== -1) {
-      this.state.activeOtaProviders[activeProviderIndex].previousQueryTimestamp = import_general.Time.nowMs;
-    } else {
-      this.state.activeOtaProviders.push({
-        location: provider,
-        previousQueryTimestamp: import_general.Time.nowMs
-      });
-    }
+    void provider;
+    this.state.activeOtaProviders = [];
   }
   /**
    * Schedule an update query after the given delay (or latest as defined as update interval if no delay is provided)
@@ -333,7 +357,7 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
       this.internal.updateQueryTimer.stop();
       this.internal.updateQueryTimer = void 0;
     }
-    if (!this.state.updatePossible || !this.state.activeOtaProviders.length) {
+    if (!this.state.updatePossible || this.internal.selectedProviderLocation === void 0 && !this.state.defaultOtaProviders.length) {
       return;
     }
     if (delay === void 0 && this.state.updateState !== import_ota_software_update_requestor.OtaSoftwareUpdateRequestor.UpdateState.Idle) {
@@ -358,19 +382,25 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
       const selected = this.internal.selectedProviderLocation;
       logger.debug(`Choosing pre-selected provider`, import_general.Diagnostic.dict(selected));
       this.internal.selectedProviderLocation = void 0;
+      if (!this.#isLocalFabric(selected.fabricIndex)) {
+        this.#logIgnoringNonLocalOtaProvider(selected);
+        this.#removeOtaProvider(selected);
+        return this.#chooseUpdateProvider();
+      }
       return selected;
     }
-    if (this.state.activeOtaProviders.length > 0) {
-      const sorted = this.state.activeOtaProviders.sort(
-        // Sort by oldest timestamp to try all over time
-        (a, b) => (a.previousQueryTimestamp ?? 0) - (b.previousQueryTimestamp ?? 0)
-      );
-      const location = sorted[0].location;
+    if (this.state.defaultOtaProviders.length > 0) {
+      const location = this.#plainProviderLocation(this.state.defaultOtaProviders[0]);
       logger.debug(
-        `Choosing active provider`,
+        `Choosing default provider`,
         (0, import_protocol.PeerAddress)({ nodeId: location.providerNodeId, fabricIndex: location.fabricIndex }),
         `ep${location.endpoint}`
       );
+      if (!this.#isLocalFabric(location.fabricIndex)) {
+        this.#logIgnoringNonLocalOtaProvider(location);
+        this.#removeOtaProvider(location);
+        return this.#chooseUpdateProvider();
+      }
       return location;
     }
     return;
@@ -378,6 +408,10 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
   /** Helper to connect to an OTA provider, initialize an OTA Provider Cluster Client, and return this endpoint */
   async #connectOtaProviderFor(location) {
     const { providerNodeId, fabricIndex, endpoint } = location;
+    if (!this.#isLocalFabric(fabricIndex)) {
+      this.#logIgnoringNonLocalOtaProvider(location);
+      throw new import_general.MatterError(`Unknown/non-local OTA provider fabricIndex=${fabricIndex}`);
+    }
     const peerAddress = (0, import_protocol.PeerAddress)({ nodeId: providerNodeId, fabricIndex });
     logger.debug(`Establish connection for OTA to ${peerAddress}`);
     const node = await import_Node.Node.forEndpoint(this.endpoint).peers.forAddress(peerAddress);
@@ -443,7 +477,18 @@ class OtaSoftwareUpdateRequestorServer extends import_OtaSoftwareUpdateRequestor
     switch (status) {
       case import_ota_software_update_provider2.OtaSoftwareUpdateProvider.Status.Busy:
       case import_ota_software_update_provider2.OtaSoftwareUpdateProvider.Status.NotAvailable:
-        if (status === import_ota_software_update_provider2.OtaSoftwareUpdateProvider.Status.Busy && this.internal.providerRetryCount <= MAX_BUSY_RETRIES) {
+        if (status === import_ota_software_update_provider2.OtaSoftwareUpdateProvider.Status.Busy) {
+          if (hamhRuntimeBusySuppressed || hamhRuntimeBusyRetryCount >= MAX_BUSY_RETRIES) {
+            hamhRuntimeBusySuppressed = true;
+            logger.debug(
+              `Busy-suppressed: OTA provider remained Busy after ${hamhRuntimeBusyRetryCount} runtime retries; no further Busy scheduling.`,
+              import_general.Diagnostic.dict(providerLocation)
+            );
+            this.#markActiveOtaProviderNoUpdate(providerLocation);
+            this.#resetStateToIdle();
+            return;
+          }
+          hamhRuntimeBusyRetryCount++;
           this.#updateState(
             import_ota_software_update_requestor.OtaSoftwareUpdateRequestor.UpdateState.DelayedOnQuery,
             import_ota_software_update_requestor.OtaSoftwareUpdateRequestor.ChangeReason.DelayByProvider
diff --git a/dist/cjs/node/server/InteractionServer.js b/dist/cjs/node/server/InteractionServer.js
index 12ded45c5f73e47d17974dc2b39fca92135771ba..ae45a816f5490ed1445d3b2fb6acf7c33a7b6e5f 100644
--- a/dist/cjs/node/server/InteractionServer.js
+++ b/dist/cjs/node/server/InteractionServer.js
@@ -80,6 +80,49 @@ var import_ServerSubscription = require("./ServerSubscription.js");
  * SPDX-License-Identifier: Apache-2.0
  */
 const logger = import_general.Logger.get("InteractionServer");
+const hamhReadClusterNames = /* @__PURE__ */ new Map([
+  [29, "Descriptor"],
+  [31, "AccessControl"],
+  [40, "BasicInformation"],
+  [42, "OtaSoftwareUpdateRequestor"],
+  [48, "GeneralCommissioning"],
+  [49, "NetworkCommissioning"],
+  [51, "GeneralDiagnostics"],
+  [60, "AdministratorCommissioning"],
+  [62, "OperationalCredentials"],
+  [63, "GroupKeyManagement"],
+  [70, "IcdManagement"]
+]);
+const hamhReadAttributeNames = /* @__PURE__ */ new Map([
+  [29, /* @__PURE__ */ new Map([[0, "deviceTypeList"], [1, "serverList"], [2, "clientList"], [3, "partsList"], [5, "endpointUniqueId"]])],
+  [-1, /* @__PURE__ */ new Map([[65528, "generatedCommandList"], [65529, "acceptedCommandList"], [65531, "attributeList"], [65532, "featureMap"], [65533, "clusterRevision"]])]
+]);
+function hamhFormatReadId(value) {
+  return value === void 0 ? "<wildcard>" : `0x${Number(value).toString(16)}`;
+}
+function hamhReadName(map, id) {
+  return id === void 0 ? "<wildcard>" : (map.get(Number(id)) ?? `0x${Number(id).toString(16)}`);
+}
+function hamhAttributeName(clusterId, attributeId) {
+  if (attributeId === void 0) return "<wildcard>";
+  return hamhReadAttributeNames.get(Number(clusterId))?.get(Number(attributeId)) ?? hamhReadAttributeNames.get(-1)?.get(Number(attributeId)) ?? `0x${Number(attributeId).toString(16)}`;
+}
+function hamhLogReadDiagnostics(nodeId, protocol, attributeRequests) {
+  if (!attributeRequests?.length) return;
+  if (globalThis.process?.env?.HAMH_LOG_CONTROLLER_READS !== "true") return;
+  const registry = globalThis.__HAMH_MATTER_READ_DIAGNOSTICS__?.bridges;
+  const endpointMap = registry?.[String(nodeId)] ?? registry?.default;
+  for (const path of attributeRequests) {
+    const endpointId = path.endpointId;
+    const endpoint = endpointId === void 0 ? void 0 : endpointMap?.[String(endpointId)];
+    const clusterName = hamhReadName(hamhReadClusterNames, path.clusterId);
+    const attributeName = hamhAttributeName(path.clusterId, path.attributeId);
+    const name = endpoint?.name ?? endpoint?.friendlyName ?? endpoint?.registryName ?? endpoint?.endpointName ?? "<unknown>";
+    logger.debug(
+      `Alexa/Controller read endpoint=${endpointId ?? "<wildcard>"} endpointType=${endpoint?.endpointName ?? "<unknown>"} entity=${endpoint?.entityId ?? (endpointId === void 0 ? "<wildcard>" : "<unknown>")} name=${JSON.stringify(name)} deviceTypeList=${JSON.stringify(endpoint?.deviceTypeList ?? null)} cluster=${clusterName}(${hamhFormatReadId(path.clusterId)}) attribute=${attributeName}(${hamhFormatReadId(path.attributeId)}) path=${protocol.inspectPath(path)}`
+    );
+  }
+}
 function validateReadAttributesPath(path, isGroupSession = false) {
   if (isGroupSession) {
     throw new import_types.StatusResponseError("Illegal read request with group session", import_types.StatusCode.InvalidAction);
@@ -214,6 +257,7 @@ class InteractionServer {
       eventFilters,
       interactionModelRevision
     } = readRequest;
+    hamhLogReadDiagnostics(this.#node.id, this.#node.protocol, attributeRequests);
     logger.debug(() => [
       "Read",
       import_protocol.Mark.INBOUND,
diff --git a/dist/cjs/node/server/ServerSubscription.js b/dist/cjs/node/server/ServerSubscription.js
index 9c1aa6236867a1e021828704dcd3150ab99b2240..317ac4a345c0c10a95ce8a253f8a77a26bdd1460 100644
--- a/dist/cjs/node/server/ServerSubscription.js
+++ b/dist/cjs/node/server/ServerSubscription.js
@@ -120,6 +120,7 @@ class ServerSubscription {
   #peerAddress;
   #sendNextUpdateImmediately = false;
   #sendUpdateErrorCounter = 0;
+  #sendRetryBackoffTimer;
   #currentUpdatePromise;
   constructor(options) {
     const { id, context, request, subscriptionOptions, useAsMaxInterval, useAsSendInterval } = options;
@@ -193,6 +194,7 @@ class ServerSubscription {
   }
   async handlePeerCancel() {
     this.#isCanceledByPeer = true;
+    this.#clearPendingRetryState(true);
     await this.close();
   }
   #determineSendingIntervals(subscriptionMinInterval, subscriptionMaxInterval, subscriptionRandomizationWindow) {
@@ -317,6 +319,14 @@ class ServerSubscription {
     ).start();
   }
   #triggerSendUpdate(onlyWithData = false, session) {
+    if (this.#isClosed || this.#isCanceledByPeer) {
+      return;
+    }
+    if (this.#sendRetryBackoffTimer?.isRunning) {
+      logger.debug("Sending update retry backoff active, coalescing update ...");
+      this.#sendNextUpdateImmediately = true;
+      return;
+    }
     if (this.#currentUpdatePromise !== void 0) {
       logger.debug("Sending update already in progress, delaying update ...");
       this.#sendNextUpdateImmediately = true;
@@ -324,6 +334,53 @@ class ServerSubscription {
     }
     this.#currentUpdatePromise = this.#sendUpdate(onlyWithData, session).catch((error) => logger.warn("Sending subscription update failed:", error)).finally(() => this.#currentUpdatePromise = void 0);
   }
+  #clearPendingRetryState(dropOutstandingUpdates = false) {
+    this.#sendRetryBackoffTimer?.stop();
+    this.#sendRetryBackoffTimer = void 0;
+    this.#sendNextUpdateImmediately = false;
+    if (dropOutstandingUpdates) {
+      this.#outstandingAttributeUpdates = void 0;
+      this.#outstandingEventsMinNumber = void 0;
+    }
+  }
+  #isRetryableSendError(error) {
+    return error instanceof import_general.NoResponseTimeoutError || error instanceof import_general.NetworkError || error instanceof import_protocol.SessionClosedError;
+  }
+  #currentRetryBackoffDelay() {
+    if (this.#sendUpdateErrorCounter <= 1) return (0, import_general.Seconds)(5);
+    if (this.#sendUpdateErrorCounter === 2) return (0, import_general.Seconds)(10);
+    return (0, import_general.Seconds)(30);
+  }
+  #scheduleRetryBackoff() {
+    const delay = this.#currentRetryBackoffDelay();
+    this.#sendRetryBackoffTimer?.stop();
+    this.#sendRetryBackoffTimer = import_general.Time.getTimer(`Subscription ${this.idStr} retry backoff`, delay, () => {
+      this.#sendRetryBackoffTimer = void 0;
+      if (this.#isClosed || this.#isCanceledByPeer || !this.#sendUpdatesActivated) {
+        this.#clearPendingRetryState(true);
+        return;
+      }
+      this.#sendNextUpdateImmediately = false;
+      this.#triggerSendUpdate(true);
+    }).start();
+    logger.debug(`Delaying subscription ${this.idStr} update retry by ${(0, import_general.Millis)(delay)}ms`);
+  }
+  #restoreOutstandingUpdates(attributeFilter, eventsMinNumber) {
+    if (attributeFilter !== void 0) {
+      for (const [endpointId, clusters] of Object.entries(attributeFilter)) {
+        for (const [clusterId, attributes] of Object.entries(clusters)) {
+          this.#addOutstandingAttributes(
+            (0, import_types.EndpointNumber)(parseInt(endpointId)),
+            (0, import_types.ClusterId)(parseInt(clusterId)),
+            Array.from(attributes)
+          );
+        }
+      }
+    }
+    if (eventsMinNumber !== void 0) {
+      this.#outstandingEventsMinNumber = eventsMinNumber;
+    }
+  }
   /**
    * Determine all attributes that have changed since the last update and send them out to the subscriber.
    */
@@ -346,6 +403,8 @@ class ServerSubscription {
             const sending = __using(_stack, updating?.join("sending"));
             if (await this.#sendUpdateMessage(sending, attributeFilter, eventsMinNumber, onlyWithData, session)) {
               this.#sendUpdateErrorCounter = 0;
+              this.#sendRetryBackoffTimer?.stop();
+              this.#sendRetryBackoffTimer = void 0;
             }
           } catch (_) {
             var _error = _, _hasError = true;
@@ -357,45 +416,24 @@ class ServerSubscription {
             return;
           }
           this.#sendUpdateErrorCounter++;
-          logger.info(
-            `Error sending subscription update message (error count=${this.#sendUpdateErrorCounter}):`,
-            error instanceof import_general.MatterError && error.message || error
-          );
-          if (this.#sendUpdateErrorCounter <= 2) {
-            if (attributeFilter !== void 0) {
-              for (const [endpointId, clusters] of Object.entries(attributeFilter)) {
-                for (const [clusterId, attributes] of Object.entries(clusters)) {
-                  this.#addOutstandingAttributes(
-                    (0, import_types.EndpointNumber)(parseInt(endpointId)),
-                    (0, import_types.ClusterId)(parseInt(clusterId)),
-                    Array.from(attributes)
-                  );
-                }
-              }
-            }
-            if (eventsMinNumber !== void 0) {
-              this.#outstandingEventsMinNumber = eventsMinNumber;
-            }
-          } else {
-            logger.info(
-              `Sending update failed 3 times in a row, canceling subscription ${this.idStr} and let controller subscribe again.`
+            const logSubscriptionUpdateError = this.#sendUpdateErrorCounter >= 3 ? logger.warn.bind(logger) : logger.debug.bind(logger);
+            logSubscriptionUpdateError(
+              `Error sending subscription update message (error count=${this.#sendUpdateErrorCounter}):`,
+              error instanceof import_general.MatterError && error.message || error
             );
+          if (this.#isRetryableSendError(error)) {
+            this.#restoreOutstandingUpdates(attributeFilter, eventsMinNumber);
+            this.#sendNextUpdateImmediately = true;
+            this.#scheduleRetryBackoff();
+            break;
+          } else if (this.#sendUpdateErrorCounter <= 2) {
+            this.#restoreOutstandingUpdates(attributeFilter, eventsMinNumber);
+          } else {
+              logger.warn(
+                `Sending update failed 3 times in a row, canceling subscription ${this.idStr} and let controller subscribe again.`
+              );
             this.#sendNextUpdateImmediately = false;
-            if (error instanceof import_general.NoResponseTimeoutError || error instanceof import_general.NetworkError || error instanceof import_protocol.SessionClosedError) {
-              var _stack2 = [];
-              try {
-                const _messaging = __using(_stack2, updating?.join("canceling"));
-                this.#isCanceledByPeer = true;
-                await this.#cancel();
-                break;
-              } catch (_2) {
-                var _error2 = _2, _hasError2 = true;
-              } finally {
-                __callDispose(_stack2, _error2, _hasError2);
-              }
-            } else {
-              throw error;
-            }
+            throw error;
           }
         }
         if (!this.#sendNextUpdateImmediately) {
@@ -577,6 +615,8 @@ class ServerSubscription {
     }
     this.#updateTimer.stop();
     this.#sendDelayTimer.stop();
+    this.#sendRetryBackoffTimer?.stop();
+    this.#sendRetryBackoffTimer = void 0;
     this.session.subscriptions.delete(this);
     logger.debug(this.session.via, "Deleted subscription", import_general.hex.fixed(this.subscriptionId, 8));
     this.cancelled.emit(this);
@@ -679,11 +719,12 @@ class ServerSubscription {
       var _stack3 = [];
       try {
         if (import_types.StatusResponseError.is(error, import_types.StatusCode.InvalidSubscription, import_types.StatusCode.Failure)) {
-          logger.info(`Subscription ${this.idStr} cancelled by peer`);
+          logger.debug(`Subscription ${this.idStr} cancelled by peer`);
           this.#isCanceledByPeer = true;
+          this.#clearPendingRetryState(true);
         } else {
           import_types.StatusResponseError.accept(error);
-          logger.info(`Subscription ${this.idStr} update failed:`, error);
+          logger.warn(`Subscription ${this.idStr} update failed:`, error);
         }
         const _canceling = __using(_stack3, lifetime?.join("canceling"));
         await this.#cancel();
diff --git a/dist/esm/behavior/system/commissioning/CommissioningServer.js b/dist/esm/behavior/system/commissioning/CommissioningServer.js
index 8c49c246e1c8225d1668f1540724554f0c6134cf..190cc752bda539fd7366ecbf9243fc2c8d582638 100644
--- a/dist/esm/behavior/system/commissioning/CommissioningServer.js
+++ b/dist/esm/behavior/system/commissioning/CommissioningServer.js
@@ -122,7 +122,10 @@ class CommissioningServer extends Behavior {
     }
   }
   #triggerFactoryReset() {
-    this.env.runtime.add(this.endpoint.erase().catch((e) => MutexClosedError.accept(e)));
+    logger.info("delaying factory reset after last fabric removal by 30s");
+    this.env.runtime.add(
+      new Promise((resolve) => setTimeout(resolve, 3e4)).then(() => this.endpoint.erase()).catch((e) => MutexClosedError.accept(e))
+    );
   }
   #monitorFailsafe(failsafe) {
     if (this.internal.unregisterFailsafeListener) {
@@ -181,6 +184,7 @@ class CommissioningServer extends Behavior {
       );
     }
     await this.env.get(DeviceCommissioner).allowBasicCommissioning();
+    logger.info("commissioning window opened after reset/remove");
     this.initiateCommissioning();
   }
   /**
diff --git a/dist/esm/behaviors/descriptor/DescriptorServer.js b/dist/esm/behaviors/descriptor/DescriptorServer.js
index 9c40593ff872cce8d40edcd4f01d0399a9461721..ee99c02915f9d648b1d75e45eead1c1a2374f35c 100644
--- a/dist/esm/behaviors/descriptor/DescriptorServer.js
+++ b/dist/esm/behaviors/descriptor/DescriptorServer.js
@@ -144,7 +144,7 @@ class DescriptorServer extends DescriptorBehavior {
     }
     numbers.sort();
     if (this.state.partsList.length === numbers.length) {
-      let i = numbers.length;
+      let i = 0;
       for (; i < numbers.length; i++) {
         if (this.state.partsList[i] !== numbers[i]) {
           break;
@@ -163,9 +163,12 @@ class DescriptorServer extends DescriptorBehavior {
    */
   get #serverList() {
     const list = new Array();
-    for (const type of Object.values(this.endpoint.behaviors.supported)) {
+    if (this.endpoint.number === 0) {
+      return [40, 31, 63, 48, 60, 62, 51, 49, 42, 70, 29];
+    }
+    for (const type of this.endpoint.behaviors.active) {
       const clusterId = type.cluster?.id;
-      if (clusterId) {
+      if (clusterId !== void 0) {
         list.push(clusterId);
       }
     }
diff --git a/dist/esm/behaviors/operational-credentials/OperationalCredentialsServer.js b/dist/esm/behaviors/operational-credentials/OperationalCredentialsServer.js
index 18752f0ca2c856445add4ecabea28fdb0818e825..e88d5220d72a3c0a138fe66ea5e61d008a4456f5 100644
--- a/dist/esm/behaviors/operational-credentials/OperationalCredentialsServer.js
+++ b/dist/esm/behaviors/operational-credentials/OperationalCredentialsServer.js
@@ -48,6 +48,7 @@ import {
 import { OperationalCredentialsBehavior } from "./OperationalCredentialsBehavior.js";
 import { VendorIdVerification } from "./VendorIdVerification.js";
 const logger = Logger.get("OperationalCredentials");
+const hamhLastFabricRemovalCleanup = /* @__PURE__ */ new WeakMap();
 OperationalCredentials.Cluster.commands = {
   ...OperationalCredentials.Cluster.commands,
   attestationRequest: Command(
@@ -308,6 +309,7 @@ class OperationalCredentialsServer extends OperationalCredentialsBehavior {
   }
   async removeFabric({ fabricIndex }) {
     assertRemoteActor(this.context);
+    logger.info(`removeFabric requested fabricIndex=${fabricIndex}`);
     const fabric = this.env.get(FabricManager).maybeFor(fabricIndex);
     if (fabric === void 0) {
       return {
@@ -316,7 +318,29 @@ class OperationalCredentialsServer extends OperationalCredentialsBehavior {
       };
     }
     await this.context.transaction.rollback();
-    await fabric.leave(this.context.exchange);
+    const fabricManager = this.env.get(FabricManager);
+    if (fabricManager.fabrics.length === 1) {
+      const activeCleanup = hamhLastFabricRemovalCleanup.get(fabricManager);
+      if (activeCleanup !== void 0) {
+        logger.info(`last fabric removal already in progress fabricIndex=${fabricIndex}`);
+        return {
+          statusCode: OperationalCredentials.NodeOperationalCertStatus.Ok,
+          fabricIndex
+        };
+      }
+      logger.info(`delaying last fabric removal cleanup by 5s fabricIndex=${fabricIndex}`);
+      const cleanup = new Promise((resolve) => setTimeout(resolve, 5e3)).then(() => fabric.leave()).catch((error) => {
+        logger.warn(`removeFabric cleanup for fabricIndex=${fabricIndex} completed with stale session errors`, error);
+      }).finally(() => hamhLastFabricRemovalCleanup.delete(fabricManager));
+      hamhLastFabricRemovalCleanup.set(fabricManager, cleanup);
+    } else {
+      try {
+        await fabric.leave(this.context.exchange);
+      } catch (error) {
+        logger.warn(`removeFabric cleanup for fabricIndex=${fabricIndex} completed with stale session errors`, error);
+      }
+    }
+    logger.info(`removeFabric succeeded statusCode=0 fabricIndex=${fabricIndex}`);
     return {
       statusCode: OperationalCredentials.NodeOperationalCertStatus.Ok,
       fabricIndex
diff --git a/dist/esm/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.js b/dist/esm/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.js
index 2671a1416a2b357a1102327fa1712665f9fa3388..4db9c0a347c6e420b10838920f405e65516f7bc1 100644
--- a/dist/esm/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.js
+++ b/dist/esm/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.js
@@ -42,6 +42,8 @@ const logger = Logger.get("OtaSoftwareUpdateRequestorServer");
 const OTA_BDX_MAX_BLOCK_SIZE_TCP = 8192;
 const OTA_BDX_MAX_BLOCK_SIZE_NON_TCP = 1024;
 const MAX_BUSY_RETRIES = 3;
+let hamhRuntimeBusyRetryCount = 0;
+let hamhRuntimeBusySuppressed = false;
 var ScheduleReason = /* @__PURE__ */ ((ScheduleReason2) => {
   ScheduleReason2[ScheduleReason2["NewQuery"] = 0] = "NewQuery";
   ScheduleReason2[ScheduleReason2["NoUpdateAvailable"] = 1] = "NoUpdateAvailable";
@@ -83,6 +85,7 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
       this.state.updateState = OtaSoftwareUpdateRequestor.UpdateState.Idle;
       this.state.updateStateProgress = null;
     }
+    this.state.activeOtaProviders = [];
     (await this.agent.load(DescriptorServer)).addDeviceTypes("OtaRequestor");
     this.reactTo(this.events.updatePossible$Changed, this.#enableOrDisableUpdates);
     this.reactTo(this.events.defaultOtaProviders$Changing, this.#assertDefaultOtaProviders);
@@ -109,17 +112,6 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
   }
   /** When we come back online after an update, we notify that the update was applied */
   async #online() {
-    if (this.state.activeOtaProviders.length === 0 && this.state.defaultOtaProviders.length > 0) {
-      for (const provider of this.state.defaultOtaProviders) {
-        await this.#addActiveOtaProvider(provider);
-      }
-      logger.debug(`No active OTA providers, adding ${this.state.activeOtaProviders.length} default providers`);
-    }
-    if (this.state.activeOtaProviders.length > 0) {
-      this.#scheduleUpdateQuery(
-        this.state.updateInProgressDetails === null ? Seconds(Math.floor(Math.random() * 599) + 120) : void 0
-      );
-    }
     if (this.state.updateInProgressDetails !== null) {
       await this.#handlePreviousUpdateOnStart(this.state.updateInProgressDetails);
       this.state.updateInProgressDetails = null;
@@ -242,7 +234,6 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
       );
     }
     const provider = { fabricIndex, providerNodeId, endpoint };
-    await this.#addActiveOtaProvider(provider, metadataForNode);
     logger.info(
       `${announcementReason === OtaSoftwareUpdateRequestor.AnnouncementReason.UrgentUpdateAvailable ? "Urgent " : ""}OTA Provider announcement received:`,
       Diagnostic.dict({
@@ -251,8 +242,19 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
         vendorId
       })
     );
-    const peerAddress = PeerAddress({ nodeId: providerNodeId, fabricIndex });
-    await Node.forEndpoint(this.endpoint).peers.forAddress(peerAddress);
+    if (!this.#isLocalFabric(fabricIndex)) {
+      this.#logIgnoringNonLocalOtaProvider(provider);
+      return;
+    }
+    await this.#addActiveOtaProvider(provider, metadataForNode);
+    if (hamhRuntimeBusySuppressed) {
+      logger.debug(
+        `Ignoring OTA provider announcement because Busy retries are suppressed for this bridge runtime.`,
+        Diagnostic.dict(provider)
+      );
+      this.#markActiveOtaProviderNoUpdate(provider);
+      return;
+    }
     if (announcementReason !== OtaSoftwareUpdateRequestor.AnnouncementReason.SimpleAnnouncement) {
       const delay = Seconds(Math.floor(Math.random() * 599) + 1);
       logger.info(`Scheduling urgent update query in`, delay);
@@ -261,37 +263,69 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
       this.#scheduleUpdateQuery();
     }
   }
-  /** Adds or updates an active OTA provider entry for a fabric index */
-  async #addActiveOtaProvider(provider, metadataForNode) {
-    const { fabricIndex, providerNodeId } = provider;
-    const activeProviderIndex = this.state.activeOtaProviders.findIndex(
-      (p) => p.location.fabricIndex === fabricIndex && p.location.providerNodeId === providerNodeId
+  #locationKey({ fabricIndex, providerNodeId, endpoint }) {
+    return `${fabricIndex}:${providerNodeId}:${endpoint}`;
+  }
+  #isSameProviderLocation(a, b) {
+    return this.#locationKey(a) === this.#locationKey(b);
+  }
+  #plainProviderLocation({ fabricIndex, providerNodeId, endpoint }) {
+    return { fabricIndex, providerNodeId, endpoint };
+  }
+  #isLocalFabric(fabricIndex) {
+    const serverNode = Node.forEndpoint(this.endpoint);
+    return Object.values(serverNode.state.commissioning.fabrics).some((fabric) => fabric.fabricIndex === fabricIndex);
+  }
+  #logIgnoringNonLocalOtaProvider({ fabricIndex, providerNodeId, endpoint }) {
+    logger.warn(
+      `Ignoring OTA provider announcement for unknown/non-local fabricIndex=${fabricIndex}, providerNodeId=${providerNodeId}, endpoint=${endpoint}`
     );
-    const activeProviderEntry = {
-      location: provider,
-      previousQueryTimestamp: null,
-      // We set to null to try this provider first next time
-      metadataForNode
-    };
-    if (activeProviderIndex === -1) {
-      this.state.activeOtaProviders.push(activeProviderEntry);
-    } else {
-      this.state.activeOtaProviders[activeProviderIndex] = activeProviderEntry;
+  }
+  #preferOtaProviderEntry(candidate, current) {
+    const candidateTimestamp = candidate.previousQueryTimestamp;
+    const currentTimestamp = current.previousQueryTimestamp;
+    if (candidateTimestamp === null) {
+      return false;
+    }
+    if (currentTimestamp === null) {
+      return true;
+    }
+    return candidateTimestamp > currentTimestamp;
+  }
+  #deduplicateActiveOtaProviders() {
+    const deduplicated = /* @__PURE__ */ new Map();
+    for (const provider of this.state.activeOtaProviders) {
+      const key = this.#locationKey(provider.location);
+      const current = deduplicated.get(key);
+      if (current === void 0 || this.#preferOtaProviderEntry(provider, current)) {
+        deduplicated.set(key, provider);
+      }
+    }
+    if (deduplicated.size !== this.state.activeOtaProviders.length) {
+      this.state.activeOtaProviders = [...deduplicated.values()];
+      logger.info(`Deduplicated active OTA providers to ${this.state.activeOtaProviders.length} entries.`);
+    }
+  }
+  /** Adds or updates an active OTA provider entry for a provider location */
+  async #addActiveOtaProvider(provider, metadataForNode) {
+    void metadataForNode;
+    const plainProvider = this.#plainProviderLocation(provider);
+    const { fabricIndex, providerNodeId } = plainProvider;
+    if (!this.#isLocalFabric(fabricIndex)) {
+      this.#logIgnoringNonLocalOtaProvider(plainProvider);
+      return;
     }
+    this.state.activeOtaProviders = [];
+    this.internal.selectedProviderLocation = plainProvider;
     const peerAddress = PeerAddress({ nodeId: providerNodeId, fabricIndex });
     await Node.forEndpoint(this.endpoint).peers.forAddress(peerAddress);
   }
   /** Removes an OTA provider from the active and default list */
   #removeOtaProvider(provider) {
-    const { fabricIndex, providerNodeId } = provider;
-    const activeProviderIndex = this.state.activeOtaProviders.findIndex(
-      (p) => p.location.fabricIndex === fabricIndex && p.location.providerNodeId === providerNodeId
-    );
-    if (activeProviderIndex !== -1) {
-      this.state.activeOtaProviders.splice(activeProviderIndex, 1);
-    }
+    const { fabricIndex, providerNodeId, endpoint } = provider;
+    this.state.activeOtaProviders = [];
     const defaultProviderIndex = this.state.defaultOtaProviders.findIndex(
-      (p) => p.fabricIndex === fabricIndex && p.providerNodeId === providerNodeId
+      (p) => p.fabricIndex === fabricIndex && p.providerNodeId === providerNodeId && p.endpoint === endpoint
     );
     if (defaultProviderIndex !== -1) {
       this.state.defaultOtaProviders.splice(defaultProviderIndex, 1);
@@ -299,18 +333,8 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
   }
   /**  Marks an OTA provider as active but with no update available (timestamp 0) */
   #markActiveOtaProviderNoUpdate(provider) {
-    const { fabricIndex, providerNodeId } = provider;
-    const activeProviderIndex = this.state.activeOtaProviders.findIndex(
-      (p) => p.location.fabricIndex === fabricIndex && p.location.providerNodeId === providerNodeId
-    );
-    if (activeProviderIndex !== -1) {
-      this.state.activeOtaProviders[activeProviderIndex].previousQueryTimestamp = Time.nowMs;
-    } else {
-      this.state.activeOtaProviders.push({
-        location: provider,
-        previousQueryTimestamp: Time.nowMs
-      });
-    }
+    void provider;
+    this.state.activeOtaProviders = [];
   }
   /**
    * Schedule an update query after the given delay (or latest as defined as update interval if no delay is provided)
@@ -333,7 +357,7 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
       this.internal.updateQueryTimer.stop();
       this.internal.updateQueryTimer = void 0;
     }
-    if (!this.state.updatePossible || !this.state.activeOtaProviders.length) {
+    if (!this.state.updatePossible || this.internal.selectedProviderLocation === void 0 && !this.state.defaultOtaProviders.length) {
       return;
     }
     if (delay === void 0 && this.state.updateState !== OtaSoftwareUpdateRequestor.UpdateState.Idle) {
@@ -358,19 +382,25 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
       const selected = this.internal.selectedProviderLocation;
       logger.debug(`Choosing pre-selected provider`, Diagnostic.dict(selected));
       this.internal.selectedProviderLocation = void 0;
+      if (!this.#isLocalFabric(selected.fabricIndex)) {
+        this.#logIgnoringNonLocalOtaProvider(selected);
+        this.#removeOtaProvider(selected);
+        return this.#chooseUpdateProvider();
+      }
       return selected;
     }
-    if (this.state.activeOtaProviders.length > 0) {
-      const sorted = this.state.activeOtaProviders.sort(
-        // Sort by oldest timestamp to try all over time
-        (a, b) => (a.previousQueryTimestamp ?? 0) - (b.previousQueryTimestamp ?? 0)
-      );
-      const location = sorted[0].location;
+    if (this.state.defaultOtaProviders.length > 0) {
+      const location = this.#plainProviderLocation(this.state.defaultOtaProviders[0]);
       logger.debug(
-        `Choosing active provider`,
+        `Choosing default provider`,
         PeerAddress({ nodeId: location.providerNodeId, fabricIndex: location.fabricIndex }),
         `ep${location.endpoint}`
       );
+      if (!this.#isLocalFabric(location.fabricIndex)) {
+        this.#logIgnoringNonLocalOtaProvider(location);
+        this.#removeOtaProvider(location);
+        return this.#chooseUpdateProvider();
+      }
       return location;
     }
     return;
@@ -378,6 +408,10 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
   /** Helper to connect to an OTA provider, initialize an OTA Provider Cluster Client, and return this endpoint */
   async #connectOtaProviderFor(location) {
     const { providerNodeId, fabricIndex, endpoint } = location;
+    if (!this.#isLocalFabric(fabricIndex)) {
+      this.#logIgnoringNonLocalOtaProvider(location);
+      throw new MatterError(`Unknown/non-local OTA provider fabricIndex=${fabricIndex}`);
+    }
     const peerAddress = PeerAddress({ nodeId: providerNodeId, fabricIndex });
     logger.debug(`Establish connection for OTA to ${peerAddress}`);
     const node = await Node.forEndpoint(this.endpoint).peers.forAddress(peerAddress);
@@ -443,7 +477,18 @@ class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestorBehavio
     switch (status) {
       case OtaSoftwareUpdateProvider.Status.Busy:
       case OtaSoftwareUpdateProvider.Status.NotAvailable:
-        if (status === OtaSoftwareUpdateProvider.Status.Busy && this.internal.providerRetryCount <= MAX_BUSY_RETRIES) {
+        if (status === OtaSoftwareUpdateProvider.Status.Busy) {
+          if (hamhRuntimeBusySuppressed || hamhRuntimeBusyRetryCount >= MAX_BUSY_RETRIES) {
+            hamhRuntimeBusySuppressed = true;
+            logger.debug(
+              `Busy-suppressed: OTA provider remained Busy after ${hamhRuntimeBusyRetryCount} runtime retries; no further Busy scheduling.`,
+              Diagnostic.dict(providerLocation)
+            );
+            this.#markActiveOtaProviderNoUpdate(providerLocation);
+            this.#resetStateToIdle();
+            return;
+          }
+          hamhRuntimeBusyRetryCount++;
           this.#updateState(
             OtaSoftwareUpdateRequestor.UpdateState.DelayedOnQuery,
             OtaSoftwareUpdateRequestor.ChangeReason.DelayByProvider
diff --git a/dist/esm/node/server/InteractionServer.js b/dist/esm/node/server/InteractionServer.js
index 795d043467508a7ca061d3651cbecb3c5bcbec64..15bc6647c71925540f0595f2f2a490f553994630 100644
--- a/dist/esm/node/server/InteractionServer.js
+++ b/dist/esm/node/server/InteractionServer.js
@@ -89,6 +89,49 @@ import {
 import { OnlineServerInteraction } from "./OnlineServerInteraction.js";
 import { ServerSubscription, ServerSubscriptionConfig } from "./ServerSubscription.js";
 const logger = Logger.get("InteractionServer");
+const hamhReadClusterNames = /* @__PURE__ */ new Map([
+  [29, "Descriptor"],
+  [31, "AccessControl"],
+  [40, "BasicInformation"],
+  [42, "OtaSoftwareUpdateRequestor"],
+  [48, "GeneralCommissioning"],
+  [49, "NetworkCommissioning"],
+  [51, "GeneralDiagnostics"],
+  [60, "AdministratorCommissioning"],
+  [62, "OperationalCredentials"],
+  [63, "GroupKeyManagement"],
+  [70, "IcdManagement"]
+]);
+const hamhReadAttributeNames = /* @__PURE__ */ new Map([
+  [29, /* @__PURE__ */ new Map([[0, "deviceTypeList"], [1, "serverList"], [2, "clientList"], [3, "partsList"], [5, "endpointUniqueId"]])],
+  [-1, /* @__PURE__ */ new Map([[65528, "generatedCommandList"], [65529, "acceptedCommandList"], [65531, "attributeList"], [65532, "featureMap"], [65533, "clusterRevision"]])]
+]);
+function hamhFormatReadId(value) {
+  return value === void 0 ? "<wildcard>" : `0x${Number(value).toString(16)}`;
+}
+function hamhReadName(map, id) {
+  return id === void 0 ? "<wildcard>" : (map.get(Number(id)) ?? `0x${Number(id).toString(16)}`);
+}
+function hamhAttributeName(clusterId, attributeId) {
+  if (attributeId === void 0) return "<wildcard>";
+  return hamhReadAttributeNames.get(Number(clusterId))?.get(Number(attributeId)) ?? hamhReadAttributeNames.get(-1)?.get(Number(attributeId)) ?? `0x${Number(attributeId).toString(16)}`;
+}
+function hamhLogReadDiagnostics(nodeId, protocol, attributeRequests) {
+  if (!attributeRequests?.length) return;
+  if (globalThis.process?.env?.HAMH_LOG_CONTROLLER_READS !== "true") return;
+  const registry = globalThis.__HAMH_MATTER_READ_DIAGNOSTICS__?.bridges;
+  const endpointMap = registry?.[String(nodeId)] ?? registry?.default;
+  for (const path of attributeRequests) {
+    const endpointId = path.endpointId;
+    const endpoint = endpointId === void 0 ? void 0 : endpointMap?.[String(endpointId)];
+    const clusterName = hamhReadName(hamhReadClusterNames, path.clusterId);
+    const attributeName = hamhAttributeName(path.clusterId, path.attributeId);
+    const name = endpoint?.name ?? endpoint?.friendlyName ?? endpoint?.registryName ?? endpoint?.endpointName ?? "<unknown>";
+    logger.debug(
+      `Alexa/Controller read endpoint=${endpointId ?? "<wildcard>"} endpointType=${endpoint?.endpointName ?? "<unknown>"} entity=${endpoint?.entityId ?? (endpointId === void 0 ? "<wildcard>" : "<unknown>")} name=${JSON.stringify(name)} deviceTypeList=${JSON.stringify(endpoint?.deviceTypeList ?? null)} cluster=${clusterName}(${hamhFormatReadId(path.clusterId)}) attribute=${attributeName}(${hamhFormatReadId(path.attributeId)}) path=${protocol.inspectPath(path)}`
+    );
+  }
+}
 function validateReadAttributesPath(path, isGroupSession = false) {
   if (isGroupSession) {
     throw new StatusResponseError("Illegal read request with group session", StatusCode.InvalidAction);
@@ -223,6 +266,7 @@ class InteractionServer {
       eventFilters,
       interactionModelRevision
     } = readRequest;
+    hamhLogReadDiagnostics(this.#node.id, this.#node.protocol, attributeRequests);
     logger.debug(() => [
       "Read",
       Mark.INBOUND,
diff --git a/dist/esm/node/server/ServerSubscription.js b/dist/esm/node/server/ServerSubscription.js
index 3da9287e7aa8111af3a07e234f0415131eb06304..a0e206451b11fb35dffc8af4bb2c24d468bc4865 100644
--- a/dist/esm/node/server/ServerSubscription.js
+++ b/dist/esm/node/server/ServerSubscription.js
@@ -123,6 +123,7 @@ class ServerSubscription {
   #peerAddress;
   #sendNextUpdateImmediately = false;
   #sendUpdateErrorCounter = 0;
+  #sendRetryBackoffTimer;
   #currentUpdatePromise;
   constructor(options) {
     const { id, context, request, subscriptionOptions, useAsMaxInterval, useAsSendInterval } = options;
@@ -196,6 +197,7 @@ class ServerSubscription {
   }
   async handlePeerCancel() {
     this.#isCanceledByPeer = true;
+    this.#clearPendingRetryState(true);
     await this.close();
   }
   #determineSendingIntervals(subscriptionMinInterval, subscriptionMaxInterval, subscriptionRandomizationWindow) {
@@ -320,6 +322,14 @@ class ServerSubscription {
     ).start();
   }
   #triggerSendUpdate(onlyWithData = false, session) {
+    if (this.#isClosed || this.#isCanceledByPeer) {
+      return;
+    }
+    if (this.#sendRetryBackoffTimer?.isRunning) {
+      logger.debug("Sending update retry backoff active, coalescing update ...");
+      this.#sendNextUpdateImmediately = true;
+      return;
+    }
     if (this.#currentUpdatePromise !== void 0) {
       logger.debug("Sending update already in progress, delaying update ...");
       this.#sendNextUpdateImmediately = true;
@@ -327,6 +337,53 @@ class ServerSubscription {
     }
     this.#currentUpdatePromise = this.#sendUpdate(onlyWithData, session).catch((error) => logger.warn("Sending subscription update failed:", error)).finally(() => this.#currentUpdatePromise = void 0);
   }
+  #clearPendingRetryState(dropOutstandingUpdates = false) {
+    this.#sendRetryBackoffTimer?.stop();
+    this.#sendRetryBackoffTimer = void 0;
+    this.#sendNextUpdateImmediately = false;
+    if (dropOutstandingUpdates) {
+      this.#outstandingAttributeUpdates = void 0;
+      this.#outstandingEventsMinNumber = void 0;
+    }
+  }
+  #isRetryableSendError(error) {
+    return error instanceof NoResponseTimeoutError || error instanceof NetworkError || error instanceof SessionClosedError;
+  }
+  #currentRetryBackoffDelay() {
+    if (this.#sendUpdateErrorCounter <= 1) return Seconds(5);
+    if (this.#sendUpdateErrorCounter === 2) return Seconds(10);
+    return Seconds(30);
+  }
+  #scheduleRetryBackoff() {
+    const delay = this.#currentRetryBackoffDelay();
+    this.#sendRetryBackoffTimer?.stop();
+    this.#sendRetryBackoffTimer = Time.getTimer(`Subscription ${this.idStr} retry backoff`, delay, () => {
+      this.#sendRetryBackoffTimer = void 0;
+      if (this.#isClosed || this.#isCanceledByPeer || !this.#sendUpdatesActivated) {
+        this.#clearPendingRetryState(true);
+        return;
+      }
+      this.#sendNextUpdateImmediately = false;
+      this.#triggerSendUpdate(true);
+    }).start();
+    logger.debug(`Delaying subscription ${this.idStr} update retry by ${Millis(delay)}ms`);
+  }
+  #restoreOutstandingUpdates(attributeFilter, eventsMinNumber) {
+    if (attributeFilter !== void 0) {
+      for (const [endpointId, clusters] of Object.entries(attributeFilter)) {
+        for (const [clusterId, attributes] of Object.entries(clusters)) {
+          this.#addOutstandingAttributes(
+            EndpointNumber(parseInt(endpointId)),
+            ClusterId(parseInt(clusterId)),
+            Array.from(attributes)
+          );
+        }
+      }
+    }
+    if (eventsMinNumber !== void 0) {
+      this.#outstandingEventsMinNumber = eventsMinNumber;
+    }
+  }
   /**
    * Determine all attributes that have changed since the last update and send them out to the subscriber.
    */
@@ -349,6 +406,8 @@ class ServerSubscription {
             const sending = __using(_stack, updating?.join("sending"));
             if (await this.#sendUpdateMessage(sending, attributeFilter, eventsMinNumber, onlyWithData, session)) {
               this.#sendUpdateErrorCounter = 0;
+              this.#sendRetryBackoffTimer?.stop();
+              this.#sendRetryBackoffTimer = void 0;
             }
           } catch (_) {
             var _error = _, _hasError = true;
@@ -360,45 +419,24 @@ class ServerSubscription {
             return;
           }
           this.#sendUpdateErrorCounter++;
-          logger.info(
-            `Error sending subscription update message (error count=${this.#sendUpdateErrorCounter}):`,
-            error instanceof MatterError && error.message || error
-          );
-          if (this.#sendUpdateErrorCounter <= 2) {
-            if (attributeFilter !== void 0) {
-              for (const [endpointId, clusters] of Object.entries(attributeFilter)) {
-                for (const [clusterId, attributes] of Object.entries(clusters)) {
-                  this.#addOutstandingAttributes(
-                    EndpointNumber(parseInt(endpointId)),
-                    ClusterId(parseInt(clusterId)),
-                    Array.from(attributes)
-                  );
-                }
-              }
-            }
-            if (eventsMinNumber !== void 0) {
-              this.#outstandingEventsMinNumber = eventsMinNumber;
-            }
-          } else {
-            logger.info(
-              `Sending update failed 3 times in a row, canceling subscription ${this.idStr} and let controller subscribe again.`
+            const logSubscriptionUpdateError = this.#sendUpdateErrorCounter >= 3 ? logger.warn.bind(logger) : logger.debug.bind(logger);
+            logSubscriptionUpdateError(
+              `Error sending subscription update message (error count=${this.#sendUpdateErrorCounter}):`,
+              error instanceof MatterError && error.message || error
             );
+          if (this.#isRetryableSendError(error)) {
+            this.#restoreOutstandingUpdates(attributeFilter, eventsMinNumber);
+            this.#sendNextUpdateImmediately = true;
+            this.#scheduleRetryBackoff();
+            break;
+          } else if (this.#sendUpdateErrorCounter <= 2) {
+            this.#restoreOutstandingUpdates(attributeFilter, eventsMinNumber);
+          } else {
+              logger.warn(
+                `Sending update failed 3 times in a row, canceling subscription ${this.idStr} and let controller subscribe again.`
+              );
             this.#sendNextUpdateImmediately = false;
-            if (error instanceof NoResponseTimeoutError || error instanceof NetworkError || error instanceof SessionClosedError) {
-              var _stack2 = [];
-              try {
-                const _messaging = __using(_stack2, updating?.join("canceling"));
-                this.#isCanceledByPeer = true;
-                await this.#cancel();
-                break;
-              } catch (_2) {
-                var _error2 = _2, _hasError2 = true;
-              } finally {
-                __callDispose(_stack2, _error2, _hasError2);
-              }
-            } else {
-              throw error;
-            }
+            throw error;
           }
         }
         if (!this.#sendNextUpdateImmediately) {
@@ -580,6 +618,8 @@ class ServerSubscription {
     }
     this.#updateTimer.stop();
     this.#sendDelayTimer.stop();
+    this.#sendRetryBackoffTimer?.stop();
+    this.#sendRetryBackoffTimer = void 0;
     this.session.subscriptions.delete(this);
     logger.debug(this.session.via, "Deleted subscription", hex.fixed(this.subscriptionId, 8));
     this.cancelled.emit(this);
@@ -682,11 +722,12 @@ class ServerSubscription {
       var _stack3 = [];
       try {
         if (StatusResponseError.is(error, StatusCode.InvalidSubscription, StatusCode.Failure)) {
-          logger.info(`Subscription ${this.idStr} cancelled by peer`);
+          logger.debug(`Subscription ${this.idStr} cancelled by peer`);
           this.#isCanceledByPeer = true;
+          this.#clearPendingRetryState(true);
         } else {
           StatusResponseError.accept(error);
-          logger.info(`Subscription ${this.idStr} update failed:`, error);
+          logger.warn(`Subscription ${this.idStr} update failed:`, error);
         }
         const _canceling = __using(_stack3, lifetime?.join("canceling"));
         await this.#cancel();
diff --git a/src/behavior/system/commissioning/CommissioningServer.ts b/src/behavior/system/commissioning/CommissioningServer.ts
index afd68c38e8109e00aacc90156957cd90dd3a17ee..c2dae6e418ec6321f137632732bc59dbdedf1f57 100644
--- a/src/behavior/system/commissioning/CommissioningServer.ts
+++ b/src/behavior/system/commissioning/CommissioningServer.ts
@@ -172,7 +172,12 @@ export class CommissioningServer extends Behavior {
     }
 
     #triggerFactoryReset() {
-        this.env.runtime.add((this.endpoint as ServerNode).erase().catch(e => MutexClosedError.accept(e)));
+        logger.info("delaying factory reset after last fabric removal by 30s");
+        this.env.runtime.add(
+            new Promise(resolve => setTimeout(resolve, 30_000))
+                .then(() => (this.endpoint as ServerNode).erase())
+                .catch(e => MutexClosedError.accept(e)),
+        );
     }
 
     #monitorFailsafe(failsafe: FailsafeContext) {
@@ -254,6 +259,7 @@ export class CommissioningServer extends Behavior {
         }
 
         await this.env.get(DeviceCommissioner).allowBasicCommissioning();
+        logger.info("commissioning window opened after reset/remove");
 
         this.initiateCommissioning();
     }
diff --git a/src/behaviors/descriptor/DescriptorServer.ts b/src/behaviors/descriptor/DescriptorServer.ts
index 33cf1714755fc69b0a14cff468bc65a90f9aa9af..71c95dc567520a1db72769125f5e6704c8806490 100644
--- a/src/behaviors/descriptor/DescriptorServer.ts
+++ b/src/behaviors/descriptor/DescriptorServer.ts
@@ -213,7 +213,7 @@ export class DescriptorServer extends DescriptorBehavior {
         // Do a quick deep equal so we can avoid updating state since the filtering on events that trigger this function
         // is rather lazy
         if (this.state.partsList.length === numbers.length) {
-            let i = numbers.length;
+            let i = 0;
             for (; i < numbers.length; i++) {
                 if (this.state.partsList[i] !== numbers[i]) {
                     break;
@@ -235,9 +235,12 @@ export class DescriptorServer extends DescriptorBehavior {
      */
     get #serverList() {
         const list = new Array<ClusterId>();
-        for (const type of Object.values(this.endpoint.behaviors.supported)) {
+        if (this.endpoint.number === 0) {
+            return [40, 31, 63, 48, 60, 62, 51, 49, 42, 70, 29] as ClusterId[];
+        }
+        for (const type of this.endpoint.behaviors.active) {
             const clusterId = (type as { cluster?: { id?: ClusterId } }).cluster?.id;
-            if (clusterId) {
+            if (clusterId !== undefined) {
                 list.push(clusterId);
             }
         }
diff --git a/src/behaviors/operational-credentials/OperationalCredentialsServer.ts b/src/behaviors/operational-credentials/OperationalCredentialsServer.ts
index dc1d41eac47ca6ba11c01ab6a181fec9e6219e08..371839fb74ab5803f2c390e1714a128d8bfdcab3 100644
--- a/src/behaviors/operational-credentials/OperationalCredentialsServer.ts
+++ b/src/behaviors/operational-credentials/OperationalCredentialsServer.ts
@@ -55,6 +55,7 @@ import { OperationalCredentialsBehavior } from "./OperationalCredentialsBehavior
 import { VendorIdVerification } from "./VendorIdVerification.js";
 
 const logger = Logger.get("OperationalCredentials");
+const hamhLastFabricRemovalCleanup = new WeakMap<FabricManager, Promise<void>>();
 
 /**
  * Monkey patching Tlv Structure of attestationRequest and csrRequest commands to prevent data validation of the nonce
@@ -406,6 +407,7 @@ export class OperationalCredentialsServer extends OperationalCredentialsBehavior
 
     override async removeFabric({ fabricIndex }: OperationalCredentials.RemoveFabricRequest) {
         assertRemoteActor(this.context);
+        logger.info(`removeFabric requested fabricIndex=${fabricIndex}`);
 
         const fabric = this.env.get(FabricManager).maybeFor(fabricIndex);
 
@@ -420,9 +422,36 @@ export class OperationalCredentialsServer extends OperationalCredentialsBehavior
         // we leave the transaction open it will cause deadlock
         await this.context.transaction.rollback();
 
-        await fabric.leave(this.context.exchange);
+        const fabricManager = this.env.get(FabricManager);
+        if (fabricManager.fabrics.length === 1) {
+            const activeCleanup = hamhLastFabricRemovalCleanup.get(fabricManager);
+            if (activeCleanup !== undefined) {
+                logger.info(`last fabric removal already in progress fabricIndex=${fabricIndex}`);
+                return {
+                    statusCode: OperationalCredentials.NodeOperationalCertStatus.Ok,
+                    fabricIndex,
+                };
+            }
+
+            logger.info(`delaying last fabric removal cleanup by 5s fabricIndex=${fabricIndex}`);
+            const cleanup = new Promise(resolve => setTimeout(resolve, 5_000))
+                .then(() => fabric.leave())
+                .catch(error => {
+                    logger.warn(`removeFabric cleanup for fabricIndex=${fabricIndex} completed with stale session errors`, error);
+                })
+                .finally(() => hamhLastFabricRemovalCleanup.delete(fabricManager));
+            hamhLastFabricRemovalCleanup.set(fabricManager, cleanup);
+        } else {
+            try {
+                await fabric.leave(this.context.exchange);
+            } catch (error) {
+                logger.warn(`removeFabric cleanup for fabricIndex=${fabricIndex} completed with stale session errors`, error);
+            }
+        }
         // The state is updated on removal via commissionedFabricChanged event, see constructor
 
+        logger.info(`removeFabric succeeded statusCode=0 fabricIndex=${fabricIndex}`);
+
         return {
             statusCode: OperationalCredentials.NodeOperationalCertStatus.Ok,
             fabricIndex,
diff --git a/src/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.ts b/src/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.ts
index 1792f4f4732a10f624353adc09b40bfca3b271bb..065b1c7084a095f1354781259c92b75bb67d1b80 100644
--- a/src/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.ts
+++ b/src/behaviors/ota-software-update-requestor/OtaSoftwareUpdateRequestorServer.ts
@@ -56,6 +56,8 @@ const OTA_BDX_MAX_BLOCK_SIZE_TCP = 8_192; // 8192 is the max defined by the spec
 const OTA_BDX_MAX_BLOCK_SIZE_NON_TCP = 1_024; // 1024 is the max defined by the spec, we use that for non-TCP channels
 
 const MAX_BUSY_RETRIES = 3; // After 3 BUSY responses in a row, we try another provider
+let hamhRuntimeBusyRetryCount = 0;
+let hamhRuntimeBusySuppressed = false;
 
 export interface ProviderLocation extends OtaSoftwareUpdateRequestor.ProviderLocation {}
 
@@ -156,6 +158,7 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
             this.state.updateState = OtaSoftwareUpdateRequestor.UpdateState.Idle;
             this.state.updateStateProgress = null;
         }
+        this.state.activeOtaProviders = [];
 
         // Make sure to add the needed device type for this endpoint
         (await this.agent.load(DescriptorServer)).addDeviceTypes("OtaRequestor");
@@ -194,25 +197,6 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
 
     /** When we come back online after an update, we notify that the update was applied */
     async #online() {
-        // If we have no active providers, but default ones, we add the default one for our fabric (if any)
-        // This makes sure that we retry removed defaltProviders at least once after a restart
-        if (this.state.activeOtaProviders.length === 0 && this.state.defaultOtaProviders.length > 0) {
-            for (const provider of this.state.defaultOtaProviders) {
-                await this.#addActiveOtaProvider(provider);
-            }
-            logger.debug(`No active OTA providers, adding ${this.state.activeOtaProviders.length} default providers`);
-        }
-
-        // Register an initial update query if we have at least one provider
-        // We choose 120s to make sure that also after a restart we respect to not query more often than 120s as per spec
-        if (this.state.activeOtaProviders.length > 0) {
-            this.#scheduleUpdateQuery(
-                this.state.updateInProgressDetails === null
-                    ? Seconds(Math.floor(Math.random() * 599) + 120)
-                    : undefined,
-            ); // when we did not get just started after an update check for updates with a random delay of 2-12mins
-        }
-
         // If we have an update in progress, check if the update was applied
         if (this.state.updateInProgressDetails !== null) {
             await this.#handlePreviousUpdateOnStart(this.state.updateInProgressDetails);
@@ -360,10 +344,6 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
         }
 
         const provider: ProviderLocation = { fabricIndex, providerNodeId, endpoint };
-
-        // Remember this provider as active to use it potentially with precedence
-        await this.#addActiveOtaProvider(provider, metadataForNode);
-
         logger.info(
             `${announcementReason === OtaSoftwareUpdateRequestor.AnnouncementReason.UrgentUpdateAvailable ? "Urgent " : ""}OTA Provider announcement received:`,
             Diagnostic.dict({
@@ -373,8 +353,22 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
             }),
         );
 
-        const peerAddress = PeerAddress({ nodeId: providerNodeId, fabricIndex });
-        await (Node.forEndpoint(this.endpoint) as ServerNode).peers.forAddress(peerAddress); // Initialize the client node and store address
+        if (!this.#isLocalFabric(fabricIndex)) {
+            this.#logIgnoringNonLocalOtaProvider(provider);
+            return;
+        }
+
+        // Remember this provider as active to use it potentially with precedence
+        await this.#addActiveOtaProvider(provider, metadataForNode);
+
+        if (hamhRuntimeBusySuppressed) {
+            logger.debug(
+                `Ignoring OTA provider announcement because Busy retries are suppressed for this bridge runtime.`,
+                Diagnostic.dict(provider),
+            );
+            this.#markActiveOtaProviderNoUpdate(provider);
+            return;
+        }
 
         if (announcementReason !== OtaSoftwareUpdateRequestor.AnnouncementReason.SimpleAnnouncement) {
             // If Urgent or UpdateAvailable, we schedule an update query earlier as we would have done before
@@ -387,23 +381,70 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
         }
     }
 
-    /** Adds or updates an active OTA provider entry for a fabric index */
-    async #addActiveOtaProvider(provider: ProviderLocation, metadataForNode?: Bytes) {
-        const { fabricIndex, providerNodeId } = provider;
-        const activeProviderIndex = this.state.activeOtaProviders.findIndex(
-            p => p.location.fabricIndex === fabricIndex && p.location.providerNodeId === providerNodeId,
+    #locationKey({ fabricIndex, providerNodeId, endpoint }: ProviderLocation) {
+        return `${fabricIndex}:${providerNodeId}:${endpoint}`;
+    }
+
+    #isSameProviderLocation(a: ProviderLocation, b: ProviderLocation) {
+        return this.#locationKey(a) === this.#locationKey(b);
+    }
+
+    #plainProviderLocation({ fabricIndex, providerNodeId, endpoint }: ProviderLocation): ProviderLocation {
+        return { fabricIndex, providerNodeId, endpoint };
+    }
+
+    #isLocalFabric(fabricIndex: FabricIndex) {
+        const serverNode = Node.forEndpoint(this.endpoint) as ServerNode;
+        return Object.values(serverNode.state.commissioning.fabrics).some(fabric => fabric.fabricIndex === fabricIndex);
+    }
+
+    #logIgnoringNonLocalOtaProvider({ fabricIndex, providerNodeId, endpoint }: ProviderLocation) {
+        logger.warn(
+            `Ignoring OTA provider announcement for unknown/non-local fabricIndex=${fabricIndex}, providerNodeId=${providerNodeId}, endpoint=${endpoint}`,
         );
-        const activeProviderEntry: ActiveProviderLocation = {
-            location: provider,
-            previousQueryTimestamp: null, // We set to null to try this provider first next time
-            metadataForNode,
-        };
-        if (activeProviderIndex === -1) {
-            this.state.activeOtaProviders.push(activeProviderEntry);
-            //this.state.activeOtaProviders = [...this.state.activeOtaProviders, activeProviderEntry];
-        } else {
-            this.state.activeOtaProviders[activeProviderIndex] = activeProviderEntry;
+    }
+
+    #preferOtaProviderEntry(candidate: ActiveProviderLocation, current: ActiveProviderLocation) {
+        const candidateTimestamp = candidate.previousQueryTimestamp;
+        const currentTimestamp = current.previousQueryTimestamp;
+
+        if (candidateTimestamp === null) {
+            return false;
+        }
+        if (currentTimestamp === null) {
+            return true;
+        }
+        return candidateTimestamp > currentTimestamp;
+    }
+
+    #deduplicateActiveOtaProviders() {
+        const deduplicated = new Map<string, ActiveProviderLocation>();
+        for (const provider of this.state.activeOtaProviders) {
+            const key = this.#locationKey(provider.location);
+            const current = deduplicated.get(key);
+            if (current === undefined || this.#preferOtaProviderEntry(provider, current)) {
+                deduplicated.set(key, provider);
+            }
+        }
+
+        if (deduplicated.size !== this.state.activeOtaProviders.length) {
+            this.state.activeOtaProviders = [...deduplicated.values()];
+            logger.info(`Deduplicated active OTA providers to ${this.state.activeOtaProviders.length} entries.`);
         }
+    }
+
+    /** Adds or updates an active OTA provider entry for a provider location */
+    async #addActiveOtaProvider(provider: ProviderLocation, metadataForNode?: Bytes) {
+        void metadataForNode;
+        const plainProvider = this.#plainProviderLocation(provider);
+        const { fabricIndex, providerNodeId } = plainProvider;
+        if (!this.#isLocalFabric(fabricIndex)) {
+            this.#logIgnoringNonLocalOtaProvider(plainProvider);
+            return;
+        }
+
+        this.state.activeOtaProviders = [];
+        this.internal.selectedProviderLocation = plainProvider;
 
         const peerAddress = PeerAddress({ nodeId: providerNodeId, fabricIndex });
         await (Node.forEndpoint(this.endpoint) as ServerNode).peers.forAddress(peerAddress); // Initialize client node and store address
@@ -411,16 +452,11 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
 
     /** Removes an OTA provider from the active and default list */
     #removeOtaProvider(provider: ProviderLocation) {
-        const { fabricIndex, providerNodeId } = provider;
-        const activeProviderIndex = this.state.activeOtaProviders.findIndex(
-            p => p.location.fabricIndex === fabricIndex && p.location.providerNodeId === providerNodeId,
-        );
-        if (activeProviderIndex !== -1) {
-            this.state.activeOtaProviders.splice(activeProviderIndex, 1);
-        }
+        const { fabricIndex, providerNodeId, endpoint } = provider;
+        this.state.activeOtaProviders = [];
 
         const defaultProviderIndex = this.state.defaultOtaProviders.findIndex(
-            p => p.fabricIndex === fabricIndex && p.providerNodeId === providerNodeId,
+            p => p.fabricIndex === fabricIndex && p.providerNodeId === providerNodeId && p.endpoint === endpoint,
         );
         if (defaultProviderIndex !== -1) {
             this.state.defaultOtaProviders.splice(defaultProviderIndex, 1);
@@ -429,18 +465,8 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
 
     /**  Marks an OTA provider as active but with no update available (timestamp 0) */
     #markActiveOtaProviderNoUpdate(provider: ProviderLocation) {
-        const { fabricIndex, providerNodeId } = provider;
-        const activeProviderIndex = this.state.activeOtaProviders.findIndex(
-            p => p.location.fabricIndex === fabricIndex && p.location.providerNodeId === providerNodeId,
-        );
-        if (activeProviderIndex !== -1) {
-            this.state.activeOtaProviders[activeProviderIndex].previousQueryTimestamp = Time.nowMs;
-        } else {
-            this.state.activeOtaProviders.push({
-                location: provider,
-                previousQueryTimestamp: Time.nowMs,
-            });
-        }
+        void provider;
+        this.state.activeOtaProviders = [];
     }
 
     /**
@@ -467,7 +493,10 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
             this.internal.updateQueryTimer = undefined;
         }
 
-        if (!this.state.updatePossible || !this.state.activeOtaProviders.length) {
+        if (
+            !this.state.updatePossible ||
+            (this.internal.selectedProviderLocation === undefined && !this.state.defaultOtaProviders.length)
+        ) {
             return;
         }
 
@@ -497,21 +526,27 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
             const selected = this.internal.selectedProviderLocation;
             logger.debug(`Choosing pre-selected provider`, Diagnostic.dict(selected));
             this.internal.selectedProviderLocation = undefined;
+            if (!this.#isLocalFabric(selected.fabricIndex)) {
+                this.#logIgnoringNonLocalOtaProvider(selected);
+                this.#removeOtaProvider(selected);
+                return this.#chooseUpdateProvider();
+            }
             return selected;
         }
 
-        // We use the most recently seen active provider first, else we fall back to the default providers
-        if (this.state.activeOtaProviders.length > 0) {
-            const sorted = this.state.activeOtaProviders.sort(
-                // Sort by oldest timestamp to try all over time
-                (a, b) => (a.previousQueryTimestamp ?? 0) - (b.previousQueryTimestamp ?? 0),
-            );
-            const location = sorted[0].location;
+        // Fall back to default providers without copying them into activeOtaProviders.
+        if (this.state.defaultOtaProviders.length > 0) {
+            const location = this.#plainProviderLocation(this.state.defaultOtaProviders[0]);
             logger.debug(
-                `Choosing active provider`,
+                `Choosing default provider`,
                 PeerAddress({ nodeId: location.providerNodeId, fabricIndex: location.fabricIndex }),
                 `ep${location.endpoint}`,
             );
+            if (!this.#isLocalFabric(location.fabricIndex)) {
+                this.#logIgnoringNonLocalOtaProvider(location);
+                this.#removeOtaProvider(location);
+                return this.#chooseUpdateProvider();
+            }
             return location;
         }
 
@@ -521,6 +556,10 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
     /** Helper to connect to an OTA provider, initialize an OTA Provider Cluster Client, and return this endpoint */
     async #connectOtaProviderFor(location: ProviderLocation) {
         const { providerNodeId, fabricIndex, endpoint } = location;
+        if (!this.#isLocalFabric(fabricIndex)) {
+            this.#logIgnoringNonLocalOtaProvider(location);
+            throw new MatterError(`Unknown/non-local OTA provider fabricIndex=${fabricIndex}`);
+        }
 
         const peerAddress = PeerAddress({ nodeId: providerNodeId, fabricIndex });
 
@@ -611,10 +650,19 @@ export class OtaSoftwareUpdateRequestorServer extends OtaSoftwareUpdateRequestor
             case OtaSoftwareUpdateProvider.Status.Busy:
             case OtaSoftwareUpdateProvider.Status.NotAvailable:
                 // If we already had 3 BUSY responses, we treat it as not available and try another provider
-                if (
-                    status === OtaSoftwareUpdateProvider.Status.Busy &&
-                    this.internal.providerRetryCount <= MAX_BUSY_RETRIES
-                ) {
+                if (status === OtaSoftwareUpdateProvider.Status.Busy) {
+                    if (hamhRuntimeBusySuppressed || hamhRuntimeBusyRetryCount >= MAX_BUSY_RETRIES) {
+                        hamhRuntimeBusySuppressed = true;
+                        logger.debug(
+                            `Busy-suppressed: OTA provider remained Busy after ${hamhRuntimeBusyRetryCount} runtime retries; no further Busy scheduling.`,
+                            Diagnostic.dict(providerLocation),
+                        );
+                        this.#markActiveOtaProviderNoUpdate(providerLocation);
+                        this.#resetStateToIdle();
+                        return;
+                    }
+
+                    hamhRuntimeBusyRetryCount++;
                     // For Busy we schedule a new query after the defined time (or min 120s) for the same provider
                     // because the provider seems to be in progress to provide an update
                     this.#updateState(
diff --git a/src/node/server/InteractionServer.ts b/src/node/server/InteractionServer.ts
index bebf17fd43ca31bf1a614f80b7cc5fb2680f1001..1ba464f1d587b4df6482fdf4caeacf7655fb8a5f 100644
--- a/src/node/server/InteractionServer.ts
+++ b/src/node/server/InteractionServer.ts
@@ -71,6 +71,80 @@ import { OnlineServerInteraction } from "./OnlineServerInteraction.js";
 import { ServerSubscription, ServerSubscriptionConfig, ServerSubscriptionContext } from "./ServerSubscription.js";
 
 const logger = Logger.get("InteractionServer");
+const hamhReadClusterNames = new Map<number, string>([
+    [0x001d, "Descriptor"],
+    [0x001f, "AccessControl"],
+    [0x0028, "BasicInformation"],
+    [0x002a, "OtaSoftwareUpdateRequestor"],
+    [0x0030, "GeneralCommissioning"],
+    [0x0031, "NetworkCommissioning"],
+    [0x0033, "GeneralDiagnostics"],
+    [0x003c, "AdministratorCommissioning"],
+    [0x003e, "OperationalCredentials"],
+    [0x003f, "GroupKeyManagement"],
+    [0x0046, "IcdManagement"],
+]);
+const hamhReadAttributeNames = new Map<number, Map<number, string>>([
+    [
+        0x001d,
+        new Map([
+            [0x0000, "deviceTypeList"],
+            [0x0001, "serverList"],
+            [0x0002, "clientList"],
+            [0x0003, "partsList"],
+            [0x0005, "endpointUniqueId"],
+        ]),
+    ],
+    [
+        -1,
+        new Map([
+            [0xfff8, "generatedCommandList"],
+            [0xfff9, "acceptedCommandList"],
+            [0xfffb, "attributeList"],
+            [0xfffc, "featureMap"],
+            [0xfffd, "clusterRevision"],
+        ]),
+    ],
+]);
+
+function hamhFormatReadId(value: unknown) {
+    return value === undefined ? "<wildcard>" : `0x${Number(value).toString(16)}`;
+}
+
+function hamhReadName(map: Map<number, string>, id: unknown) {
+    return id === undefined ? "<wildcard>" : (map.get(Number(id)) ?? `0x${Number(id).toString(16)}`);
+}
+
+function hamhAttributeName(clusterId: unknown, attributeId: unknown) {
+    if (attributeId === undefined) return "<wildcard>";
+
+    return (
+        hamhReadAttributeNames.get(Number(clusterId))?.get(Number(attributeId)) ??
+        hamhReadAttributeNames.get(-1)?.get(Number(attributeId)) ??
+        `0x${Number(attributeId).toString(16)}`
+    );
+}
+
+function hamhLogReadDiagnostics(nodeId: unknown, protocol: { inspectPath(path: unknown): string }, attributeRequests?: any[]) {
+    if (!attributeRequests?.length) return;
+    if ((globalThis as any).process?.env?.HAMH_LOG_CONTROLLER_READS !== "true") return;
+
+    const registry = (globalThis as any).__HAMH_MATTER_READ_DIAGNOSTICS__?.bridges;
+    const endpointMap = registry?.[String(nodeId)] ?? registry?.default;
+
+    for (const path of attributeRequests) {
+        const endpointId = path.endpointId;
+        const endpoint = endpointId === undefined ? undefined : endpointMap?.[String(endpointId)];
+        const clusterName = hamhReadName(hamhReadClusterNames, path.clusterId);
+        const attributeName = hamhAttributeName(path.clusterId, path.attributeId);
+        const name =
+            endpoint?.name ?? endpoint?.friendlyName ?? endpoint?.registryName ?? endpoint?.endpointName ?? "<unknown>";
+
+        logger.debug(
+            `Alexa/Controller read endpoint=${endpointId ?? "<wildcard>"} endpointType=${endpoint?.endpointName ?? "<unknown>"} entity=${endpoint?.entityId ?? (endpointId === undefined ? "<wildcard>" : "<unknown>")} name=${JSON.stringify(name)} deviceTypeList=${JSON.stringify(endpoint?.deviceTypeList ?? null)} cluster=${clusterName}(${hamhFormatReadId(path.clusterId)}) attribute=${attributeName}(${hamhFormatReadId(path.attributeId)}) path=${protocol.inspectPath(path)}`,
+        );
+    }
+}
 
 export interface PeerSubscription {
     subscriptionId: number;
@@ -269,6 +343,8 @@ export class InteractionServer implements ProtocolHandler, InteractionRecipient
             interactionModelRevision,
         } = readRequest;
 
+        hamhLogReadDiagnostics(this.#node.id, this.#node.protocol, attributeRequests);
+
         logger.debug(() => [
             "Read",
             Mark.INBOUND,
diff --git a/src/node/server/ServerSubscription.ts b/src/node/server/ServerSubscription.ts
index 13953b5a8c9f68e60942e0bebf563eb9ea866d9f..d956b17a0832806ae6a70a97c08278f6c5a255d7 100644
--- a/src/node/server/ServerSubscription.ts
+++ b/src/node/server/ServerSubscription.ts
@@ -148,6 +148,7 @@ export class ServerSubscription implements Subscription {
 
     #sendNextUpdateImmediately = false;
     #sendUpdateErrorCounter = 0;
+    #sendRetryBackoffTimer?: Timer;
     #currentUpdatePromise?: Promise<void>;
 
     constructor(options: {
@@ -243,6 +244,7 @@ export class ServerSubscription implements Subscription {
 
     async handlePeerCancel() {
         this.#isCanceledByPeer = true;
+        this.#clearPendingRetryState(true);
         await this.close();
     }
 
@@ -415,6 +417,14 @@ export class ServerSubscription implements Subscription {
     }
 
     #triggerSendUpdate(onlyWithData: boolean = false, session?: Session) {
+        if (this.#isClosed || this.#isCanceledByPeer) {
+            return;
+        }
+        if (this.#sendRetryBackoffTimer?.isRunning) {
+            logger.debug("Sending update retry backoff active, coalescing update ...");
+            this.#sendNextUpdateImmediately = true;
+            return;
+        }
         if (this.#currentUpdatePromise !== undefined) {
             logger.debug("Sending update already in progress, delaying update ...");
             this.#sendNextUpdateImmediately = true;
@@ -425,6 +435,62 @@ export class ServerSubscription implements Subscription {
             .finally(() => (this.#currentUpdatePromise = undefined));
     }
 
+    #clearPendingRetryState(dropOutstandingUpdates = false) {
+        this.#sendRetryBackoffTimer?.stop();
+        this.#sendRetryBackoffTimer = undefined;
+        this.#sendNextUpdateImmediately = false;
+        if (dropOutstandingUpdates) {
+            this.#outstandingAttributeUpdates = undefined;
+            this.#outstandingEventsMinNumber = undefined;
+        }
+    }
+
+    #isRetryableSendError(error: unknown) {
+        return (
+            error instanceof NoResponseTimeoutError ||
+            error instanceof NetworkError ||
+            error instanceof SessionClosedError
+        );
+    }
+
+    #currentRetryBackoffDelay() {
+        if (this.#sendUpdateErrorCounter <= 1) return Seconds(5);
+        if (this.#sendUpdateErrorCounter === 2) return Seconds(10);
+        return Seconds(30);
+    }
+
+    #scheduleRetryBackoff() {
+        const delay = this.#currentRetryBackoffDelay();
+        this.#sendRetryBackoffTimer?.stop();
+        this.#sendRetryBackoffTimer = Time.getTimer(`Subscription ${this.idStr} retry backoff`, delay, () => {
+            this.#sendRetryBackoffTimer = undefined;
+            if (this.#isClosed || this.#isCanceledByPeer || !this.#sendUpdatesActivated) {
+                this.#clearPendingRetryState(true);
+                return;
+            }
+            this.#sendNextUpdateImmediately = false;
+            this.#triggerSendUpdate(true);
+        }).start();
+        logger.debug(`Delaying subscription ${this.idStr} update retry by ${Millis(delay)}ms`);
+    }
+
+    #restoreOutstandingUpdates(attributeFilter?: DirtyState.ForNode, eventsMinNumber?: EventNumber) {
+        if (attributeFilter !== undefined) {
+            for (const [endpointId, clusters] of Object.entries(attributeFilter)) {
+                for (const [clusterId, attributes] of Object.entries(clusters)) {
+                    this.#addOutstandingAttributes(
+                        EndpointNumber(parseInt(endpointId)),
+                        ClusterId(parseInt(clusterId)),
+                        Array.from(attributes),
+                    );
+                }
+            }
+        }
+        if (eventsMinNumber !== undefined) {
+            this.#outstandingEventsMinNumber = eventsMinNumber; // newer number are always higher, so we can just set it
+        }
+    }
+
     /**
      * Determine all attributes that have changed since the last update and send them out to the subscriber.
      */
@@ -449,6 +515,8 @@ export class ServerSubscription implements Subscription {
                 using sending = updating?.join("sending");
                 if (await this.#sendUpdateMessage(sending, attributeFilter, eventsMinNumber, onlyWithData, session)) {
                     this.#sendUpdateErrorCounter = 0;
+                    this.#sendRetryBackoffTimer?.stop();
+                    this.#sendRetryBackoffTimer = undefined;
                 }
             } catch (error) {
                 if (this.#isClosed) {
@@ -458,45 +526,26 @@ export class ServerSubscription implements Subscription {
                 }
 
                 this.#sendUpdateErrorCounter++;
-                logger.info(
+                const logSubscriptionUpdateError =
+                    this.#sendUpdateErrorCounter >= 3 ? logger.warn.bind(logger) : logger.debug.bind(logger);
+                logSubscriptionUpdateError(
                     `Error sending subscription update message (error count=${this.#sendUpdateErrorCounter}):`,
                     (error instanceof MatterError && error.message) || error,
                 );
-                if (this.#sendUpdateErrorCounter <= 2) {
+                if (this.#isRetryableSendError(error)) {
+                    this.#restoreOutstandingUpdates(attributeFilter, eventsMinNumber);
+                    this.#sendNextUpdateImmediately = true;
+                    this.#scheduleRetryBackoff();
+                    break;
+                } else if (this.#sendUpdateErrorCounter <= 2) {
                     // fill the data back in the queue to resend with next try
-                    if (attributeFilter !== undefined) {
-                        for (const [endpointId, clusters] of Object.entries(attributeFilter)) {
-                            for (const [clusterId, attributes] of Object.entries(clusters)) {
-                                this.#addOutstandingAttributes(
-                                    EndpointNumber(parseInt(endpointId)),
-                                    ClusterId(parseInt(clusterId)),
-                                    Array.from(attributes),
-                                );
-                            }
-                        }
-                    }
-                    if (eventsMinNumber !== undefined) {
-                        this.#outstandingEventsMinNumber = eventsMinNumber; // newer number are always higher, so we can just set it
-                    }
+                    this.#restoreOutstandingUpdates(attributeFilter, eventsMinNumber);
                 } else {
-                    logger.info(
+                    logger.warn(
                         `Sending update failed 3 times in a row, canceling subscription ${this.idStr} and let controller subscribe again.`,
                     );
                     this.#sendNextUpdateImmediately = false;
-                    if (
-                        error instanceof NoResponseTimeoutError ||
-                        error instanceof NetworkError ||
-                        error instanceof SessionClosedError
-                    ) {
-                        // Let's consider this subscription as dead and wait for a reconnect.  We handle as if the
-                        // controller cancelled
-                        using _messaging = updating?.join("canceling");
-                        this.#isCanceledByPeer = true;
-                        await this.#cancel();
-                        break;
-                    } else {
-                        throw error;
-                    }
+                    throw error;
                 }
             }
 
@@ -680,6 +729,8 @@ export class ServerSubscription implements Subscription {
 
         this.#updateTimer.stop();
         this.#sendDelayTimer.stop();
+        this.#sendRetryBackoffTimer?.stop();
+        this.#sendRetryBackoffTimer = undefined;
 
         this.session.subscriptions.delete(this);
         logger.debug(this.session.via, "Deleted subscription", hex.fixed(this.subscriptionId, 8));
@@ -786,11 +837,12 @@ export class ServerSubscription implements Subscription {
             }
         } catch (error) {
             if (StatusResponseError.is(error, StatusCode.InvalidSubscription, StatusCode.Failure)) {
-                logger.info(`Subscription ${this.idStr} cancelled by peer`);
+                logger.debug(`Subscription ${this.idStr} cancelled by peer`);
                 this.#isCanceledByPeer = true;
+                this.#clearPendingRetryState(true);
             } else {
                 StatusResponseError.accept(error);
-                logger.info(`Subscription ${this.idStr} update failed:`, error);
+                logger.warn(`Subscription ${this.idStr} update failed:`, error);
             }
 
             using _canceling = lifetime?.join("canceling");
