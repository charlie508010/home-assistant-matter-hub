diff --git a/dist/cjs/interaction/FabricAccessControl.js b/dist/cjs/interaction/FabricAccessControl.js
index 074639fccda4ebf949b0a09af4deac6bb61d517c..0df34efb5925f47827d88547ada752517678cf0a 100644
--- a/dist/cjs/interaction/FabricAccessControl.js
+++ b/dist/cjs/interaction/FabricAccessControl.js
@@ -262,3 +262,4 @@ class FabricAccessControl {
   }
 }
 //# sourceMappingURL=FabricAccessControl.js.map
+
diff --git a/dist/esm/interaction/FabricAccessControl.js b/dist/esm/interaction/FabricAccessControl.js
index cb87d8227995459529a78d5b6bbbb6e7da78a1ec..d066e9d628cf3f944b97b8df7781882c4024bd20 100644
--- a/dist/esm/interaction/FabricAccessControl.js
+++ b/dist/esm/interaction/FabricAccessControl.js
@@ -68,8 +68,9 @@ class FabricAccessControl {
    */
   accessLevelsFor(context, location, endpoint) {
     if (location.cluster === void 0) {
-      logger.warn("Access control check without cluster, returning View access level");
-      return [AccessLevel.View];
+      // WORKAROUND: Grant Operate access when cluster undefined (bug in BehaviorBacking.ts)
+      logger.debug("Access control check without cluster, granting Operate access as workaround");
+      return [AccessLevel.Operate, AccessLevel.View];
     }
     if (endpoint === void 0) {
       logger.warn("Access control check without endpoint, returning View access level");
@@ -248,3 +249,4 @@ export {
   FabricAccessControl
 };
 //# sourceMappingURL=FabricAccessControl.js.map
+
diff --git a/src/interaction/FabricAccessControl.ts b/src/interaction/FabricAccessControl.ts
index 9208c5e56adb583b5bbb21492080342d25fab119..d139212abc6663f917c91b1917788dcfde9f3989 100644
--- a/src/interaction/FabricAccessControl.ts
+++ b/src/interaction/FabricAccessControl.ts
@@ -123,9 +123,11 @@ export class FabricAccessControl {
         endpoint?: AclEndpointContext,
     ): AccessLevel[] {
         if (location.cluster === undefined) {
-            // Without a cluster, internal behaviors are only accessible internally, so this is an irrelevant placeholder
-            logger.warn("Access control check without cluster, returning View access level");
-            return [AccessLevel.View];
+            // WORKAROUND: When cluster is undefined (bug in BehaviorBacking.ts), grant Operate access
+            // instead of only View access to allow attribute writes to succeed.
+            // See: https://github.com/project-chip/matter.js/issues/3106
+            logger.debug("Access control check without cluster, granting Operate access as workaround");
+            return [AccessLevel.Operate, AccessLevel.View];
         }
         if (endpoint === undefined) {
             // Without an endpoint, we can't determine access levels, ???
